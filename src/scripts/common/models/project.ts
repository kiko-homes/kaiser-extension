export interface Project {
  uid: string;
  name: string;
  slidesCount: number;
}

export interface ProjectFull {
  name: string;
  organisationUid: string;
  thumbnail: string;
  slidesCount: number;
}
